# include <stdio.h>
int main()
{
	int score[4][4], i, j;
	char team[4];
	for ( i=0 ; i<4 ; i++)
	{
	scanf("%s", Team[i]);
	}
	for ( i=0 ; i<4 ; i++)
	{
		for ( j=0 ; j<4 ; j++)
		{
			scanf("%d", %array[i][j]);
		}
	}
	for ( i=0 ; i<4 ; i++)
	{
		for ( j=0 ; j<4 && j>i ; j++)
		{
			if ( array[i][j] == array[j][i])
			{
				continue ;
			}
			if ( array[i][j] == array[j][i])
		}
	}
}
