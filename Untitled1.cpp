#include <stdio.h>
#include <math.h>
int main()
{
	int digits = 0, ans = 0, fraction, a, i, number, M_number;
	scanf("%d", &number);
	
	while ( number > 10){
		a 
	}
	printf("ans is %d", ans);
	return 0;
}
